-- migrate:up
-- Захватки объекта заводятся с экрана (ADR-024): изменение идёт в историю объекта.
-- Новые значения перечисления в транзакции миграции только добавляются; используются после неё.
alter type event_type add value if not exists 'zone_changed';

-- migrate:down
-- PostgreSQL не удаляет значения перечисления. Откат оставляет его: значение без строк
-- ничему не мешает, а строки истории с ним удалять нельзя — журнал только пополняется.
select 1;
