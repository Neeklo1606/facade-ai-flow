/** Геометрия таблицы на отрисованном листе документа: где начинаются строки и сколько места они занимают. */
export const SHEET_TABLE = { top: 0.135, bottom: 0.855, left: 0.06, right: 0.94 } as const;
